RewriteEngine On

# Write requested file
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^.*\.(css|js|png|jpg|jpeg|gif|otf|eot|svg|ttf|woff|woff2) - [END,L]

# If non-existent file, serve a 404 otherwise...
RewriteCond %{ENV:REDIRECT_STATUS} ^$
RewriteRule ^ - [R=404]
